<UserControl
    x:Class="H.ReactiveUI.Apps.Views.FileInteractionsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:H.ReactiveUI.Apps.ViewModels;assembly=H.ReactiveUI.Apps.Core"
    xmlns:views="clr-namespace:H.ReactiveUI.Apps.Views"
    xmlns:h="clr-namespace:H.ReactiveUI;assembly=H.ReactiveUI.CommonInteractions.Wpf"
    mc:Ignorable="d"
    AllowDrop="True"
    h:DragAndDropExtensions.DragFilesEnterCommand="{Binding DragFilesEnter}"
    h:DragAndDropExtensions.DragTextEnterCommand="{Binding DragTextEnter}"
    h:DragAndDropExtensions.DragLeaveCommand="{Binding DragLeave}"
    h:DragAndDropExtensions.DropFilesCommand="{Binding DropFiles}"
    h:DragAndDropExtensions.DropTextCommand="{Binding DropText}"
    >
    <UserControl.DataContext>
        <viewModels:FileInteractionsViewModel/>
    </UserControl.DataContext>

    <Grid>
        <StackPanel Orientation="Horizontal">
            <StackPanel>
                <Button Content="OpenFile" Command="{Binding OpenFile}"/>
                <Button Content="OpenFiles" Command="{Binding OpenFiles}"/>
                <Button Content="SaveFile" Command="{Binding SaveFile}"/>
                <Button Content="SaveOpenFile" Command="{Binding SaveOpenFile}"/>
                <Button Content="LaunchPath" Command="{Binding LaunchPath}"/>
                <Button Content="LaunchInTemp" Command="{Binding LaunchInTemp}"/>
                <Button Content="LaunchFolder" Command="{Binding LaunchFolder}"/>
            </StackPanel>
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Current File:"/>
                    <TextBlock Text="{Binding SelectedFile.FileName}" d:Text="None"/>
                </StackPanel>
                <TextBox
                    TextWrapping="Wrap"
                    AcceptsReturn="True"
                    Width="400"
                    Height="400"
                    Text="{Binding Content, Mode=TwoWay}"
                    />
            </StackPanel>
        </StackPanel>

        <views:PreviewDropView
            DataContext="{Binding PreviewDropViewModel}"
            />
    </Grid>
</UserControl>
