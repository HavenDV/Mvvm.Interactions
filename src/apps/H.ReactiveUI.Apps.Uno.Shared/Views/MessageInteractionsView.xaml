<views:MessageInteractionsViewBase
    x:Class="HReactiveUI.Apps.Views.MessageInteractionsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:H.ReactiveUI.Apps.ViewModels"
    xmlns:views="using:HReactiveUI.Apps.Views"
    mc:Ignorable="d"
    d:RequestedTheme="Dark"
    >
    <StackPanel>
        <Button
            Content="Message"
            Command="{x:Bind ViewModel.Message, Mode=OneTime}"
            />
        <Button
            Content="Warning"
            Command="{x:Bind ViewModel.Warning, Mode=OneTime}"
            />
        <Button
            Content="Exception"
            Command="{x:Bind ViewModel.Exception, Mode=OneTime}"
            />
        <Button
            Content="Question"
            Command="{x:Bind ViewModel.Question, Mode=OneTime}"
            />
    </StackPanel>
</views:MessageInteractionsViewBase>
